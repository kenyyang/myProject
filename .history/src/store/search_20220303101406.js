import { reqGetSearchInfo } from "../api"

const state = {}
const mutations = {}
const actions = {
    //获取search
}
const getters = {}
export default {
    state,
    mutations,
    actions,
    getters
}