import { reqGetSearchInfo } from "../api"

const state = {}
const mutations = {}
const actions = {
    //获取search3
}
const getters = {}
export default {
    state,
    mutations,
    actions,
    getters
}